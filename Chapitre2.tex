\chapter{Needs Analysis and Conceptual Study}
\section{Introduction}
The quality of any project is largely determined by the strength of its foundation. This chapter is therefore dedicated to analyzing the system requirements, starting with the functional and non-functional needs, followed by a presentation of the development tools used, and concluding with the use case diagram that captures the overall scope of the system.

\section{Need Analysis}
This section presents the functional and non-functional requirements of the Ask-Jade system, as well as the different actors interacting with the platform.

\subsection{Functional Needs}
To ensure that Ask-Jade meets the expectations of its users, it is essential to identify all the functional requirements that the system must fulfill. These requirements define the core features and interactions that the platform must provide to each category of actors.

% Ajoutez \usepackage{enumitem} dans votre préambule. 
% L'option [noitemsep, topsep=2pt] supprime l'espace entre les items et réduit l'espace supérieur.
\renewcommand{\labelitemi}{\large$\bullet$}
\begin{itemize}[noitemsep, topsep=2pt]
    \item \textbf{Profile Management:} Users must be able to edit their personal profiles within the system to keep their information up to date.
    \item \textbf{Conversation Lifecycle Management:} The system must allow consultants to manage their inquiries through a dedicated interface. This includes the ability to start a new conversation, view their historical interactions, update ongoing discussions, and delete conversations that are no longer needed.
    \item \textbf{Natural Language Interaction:} The core of the system allows consultants to ask natural language questions. The "Ask-Jade" actor is responsible for processing these queries and responding to questions accurately.
    \item \textbf{Comprehensive Document Management:} The system must provide a robust "Manage Documents" module. This includes the ability for authorized users to ingest documents and delete documents.
    \item \textbf{Metadata Validation and Correction:} Beyond automated ingestion, the system must allow Document Managers to "Manage Metadata". This includes displaying extracted information and providing tools to validate, correct, or complete document metadata to ensure high-accuracy retrieval.
    \item \textbf{User Account Administration:} The Administrator must have full control over the user base through a "Manage User Accounts" feature. This includes the ability to add new users, update existing account details, delete users, and archive user profiles.
    \item \textbf{System Auditing and Logging:} The system must automatically generate activity logs. These logs must be accessible to administrators for security auditing and system monitoring.
    \item \textbf{Performance Analytics:} To support continuous improvement, the system must allow for the analysis of user questions and retrieval performance through a dedicated "View Analytics" dashboard.
    \item \textbf{Model Management:} The Administrator must be able to manage and configure the language models (LLMs) used by the system. This includes selecting the active model for response generation, switching between different models based on performance or cost, and defining default model settings for the Ask-Jade system.
\end{itemize}

\subsection{Non-Functional Needs}
The system must satisfy several non-functional requirements to ensure efficient and reliable operation.

\begin{itemize}[noitemsep, topsep=2pt]
    \item \textbf{Ergonomics:} The system must provide an intuitive and user-friendly interface.
    \item \textbf{Performance:} The system should ensure fast response times through asynchronous processing and optimized retrieval.
    \item \textbf{Reliability:} The system must guarantee stable operation using fallback models and error-handling mechanisms.
    \item \textbf{Scalability:} The system should support increasing numbers of documents and users without performance degradation.
    \item \textbf{Security:} The system must protect sensitive data through authentication, access control, and rate limiting.
\end{itemize}

\subsection{Identification of Actors}
Before defining the functional requirements, it is essential to identify the actors who interact with the system. An actor represents a role played by a user or an external system that exchanges information with Ask-Jade.

\begin{itemize}[noitemsep, topsep=2pt]
    \item \textbf{Consultant:} The primary end-user of the platform. This actor interacts with the system to ask natural language questions, manage their conversation history, and update their personal profile.
    \item \textbf{Document Manager:} A specialized user responsible for the quality of the knowledge base. Their role includes uploading documents and, most importantly, managing, validating, and correcting metadata to ensure high-accuracy retrieval.
    \item \textbf{Administrator:} The user in charge of system governance. This role manages user accounts (adding, updating, or deleting users), monitors system security through audit logs, and configures the active language model used by the system.
    \item \textbf{Ask-Jade (System Actor):} Represented as an intelligent agent that processes user queries and generates context-aware responses based on the indexed document library.
    \item \textbf{System (Technical Actor):} An internal actor responsible for automated background tasks, such as generating logs and maintaining the document management lifecycle.
\end{itemize}

\subsection{Global Use Case Diagram}
\textbf{Figure~\ref{fig:global_use_case}} illustrates the main use cases of the system.

\subsection{Use Case Refinement}
The following subsections present the detailed refinement of the main use cases identified in the system, describing the actors, scenarios, preconditions, postconditions, and possible exceptions associated with each functionality.

\subsubsection{a- Manage Users}
\textbf{Figure~\ref{fig:uc_manage_users}} presents the use case diagram of Manage Users:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{ask-jade-use-case.jpg}
    \caption{Global Use Case Diagram}
    \label{fig:global_use_case}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{Manage Users.jpg}
    \caption{Use Case Diagram of Manage Users}
    \label{fig:uc_manage_users}
\end{figure}


\textbf{The table~\ref{tab:ref_manage_users}} provides a detailed functional breakdown of user account administration.
\begin{table}[H]
\centering
\caption{Refinement of the Use Case Manage User Accounts}
\label{tab:ref_manage_users}
\begin{tabular}{|>{\centering\arraybackslash}p{4cm}|p{10cm}|}
\hline
\textbf{Use Case} & Manage User Accounts \\ \hline
\textbf{Actor} & Administrator \\ \hline
\textbf{Precondition} & System is running, administrator is authenticated. \\ \hline
\textbf{Postcondition} & Users are successfully managed (added, updated, archived, restored, or deleted). \\ \hline
\textbf{Main Scenario} & The administrator accesses the Users section and the system displays the list of users. \\ \hline
\textbf{Alternative Scenarios} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
The administrator accesses the \textbf{Display} and the system shows the list of users :\newline
\hspace*{0.2cm}-- The administrator fills in the form and confirms to \textbf{Add User}.\newline
\hspace*{0.2cm}-- The administrator modifies the profile information and confirms to \textbf{Update User}.\newline
\hspace*{0.2cm}-- The system permanently deletes the user after confirmation (\textbf{Delete User}).\newline
The system archives the user, who remains visible with reduced opacity (\textbf{Archive User}).\newline
\hspace*{0.4cm}-- The system restores the archived user account (\textbf{Restore}).
}\\ \hline
\textbf{Exception Scenario} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
-- If not authenticated, the system redirects to the login page.\newline
-- If users cannot be retrieved, an error message is displayed.\newline
-- If a required field is missing, the system displays a validation error.\newline
-- If the email already exists, the user creation is rejected.
}\\ \hline
\end{tabular}
\end{table}


\subsubsection{b- Authenticate}
\textbf{Figure~\ref{fig:uc_authenticate}} presents the use case diagram of Authenticate User:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{authenticate.jpg}
    \caption{Use Case Diagram of Authenticate}
    \label{fig:uc_authenticate}
\end{figure}

\textbf{Table~\ref{tab:ref_authenticate}} describes the sequential scenarios for secure platform access.

\begin{table}[H]
\centering
\caption{Refinement of the Use Case Authenticate}
\label{tab:ref_authenticate}
\begin{tabular}{|>{\centering\arraybackslash}p{4cm}|p{10cm}|}
\hline
\textbf{Use Case} & Authenticate \\ \hline
\textbf{Actor} & Administrator, Consultant, Document Manager \\ \hline
\textbf{Precondition} & System is running. \\ \hline
\textbf{Postcondition} & User is successfully authenticated and redirected to the dashboard. \\ \hline
\textbf{Main Scenario} & The system displays the authentication interface. \\ \hline
\textbf{Alternative Scenarios} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
The user enters their credentials and optionally activates \textbf{Remember Me}.\newline
The user selects \textbf{Sign In}:\newline
\hspace*{0.2cm}-- The system verifies the credentials and redirects to the dashboard.\newline
The user selects \textbf{Forgot your password?}:\newline
\hspace*{0.2cm}-- The user submits their email address.\newline
\hspace*{0.2cm}-- The system sends a password reset link by email.\newline
The user follows the reset link:\newline
\hspace*{0.2cm}-- The user enters and confirms the new password.\newline
\hspace*{0.2cm}-- The system updates the password and redirects to the sign in page.
}\\ \hline
\textbf{Exception Scenario} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
-- If credentials are incorrect, an error message is displayed.\newline
-- If the account is locked after multiple failed attempts, a temporary lock message is displayed.\newline
-- If passwords do not match during reset, an error message is displayed.
}\\ \hline
\end{tabular}
\end{table}

\subsubsection{c- Edit Profile}
\textbf{Figure~\ref{fig:uc_edit_profile}} presents the use case diagram of Edit Profile:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{Edit profile.jpg}
    \caption{Use Case Diagram of Edit profile}
    \label{fig:uc_edit_profile}
\end{figure}

\textbf{Table~\ref{tab:ref_edit_profile}} outlines how actors maintain their account information.

\begin{table}[H]
\centering
\caption{Refinement of the Use Case Edit Profile}
\label{tab:ref_edit_profile}
\begin{tabular}{|>{\centering\arraybackslash}p{4cm}|p{10cm}|}
\hline
\textbf{Use Case} & Edit Profile \\ \hline
\textbf{Actor} & Administrator, Consultant, Document Manager \\ \hline
\textbf{Precondition} & User is authenticated and connected to the system. \\ \hline
\textbf{Postcondition} & User profile or password is successfully updated. \\ \hline
\textbf{Main Scenario} & The user accesses the profile menu from the header. The system displays a dropdown with available options. \\ \hline
\textbf{Alternative Scenarios} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
The user selects \textbf{Edit Profile}:\newline
\hspace*{0.2cm}-- The user updates the required fields and confirms.\newline
\hspace*{0.2cm}-- The system validates and saves the profile successfully.\newline
The user selects \textbf{Edit Password}:\newline
\hspace*{0.2cm}-- The user enters the current password, then the new password and its confirmation.\newline
\hspace*{0.2cm}-- The system verifies and updates the password successfully.
}\\ \hline
\textbf{Exception Scenario} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
-- If required fields are empty, a validation error is displayed.\newline
-- If the current password is incorrect, an error message is displayed.\newline
-- If the new password and confirmation do not match, an error message is displayed.\newline
-- If a server error occurs, an error message is displayed.
}\\ \hline
\end{tabular}
\end{table}

\subsubsection{d- Ask Questions}
\textbf{Figure~\ref{fig:uc_ask_questions}} presents the use case diagram of Ask Questions:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{ask questions.jpg}
    \caption{Use Case Diagram of Ask Questions}
    \label{fig:uc_ask_questions}
\end{figure}

\textbf{Table~\ref{tab:ref_ask_questions}} details the foundational question-answering workflow.

\begin{table}[H]
\centering
\caption{Refinement of the Use Case Ask Questions}
\label{tab:ref_ask_questions}
\begin{tabular}{|>{\centering\arraybackslash}p{4cm}|p{10cm}|}
\hline
\textbf{Use Case} & Ask Questions \\ \hline
\textbf{Actor} & Administrator, Consultant, Document Manager \\ \hline
\textbf{Precondition} & User is authenticated and has access to the chat interface. \\ \hline
\textbf{Postcondition} & The system provides an answer to the user's question. \\ \hline
\textbf{Main Scenario} & The system displays the chat interface. \\ \hline
\textbf{Alternative Scenarios} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
The user enters a question and submits it:\newline
\hspace*{0.2cm}-- The system processes the request and displays the generated response.\newline
\hspace*{0.2cm}-- The system displays sources related to the answer.
}\\ \hline
\textbf{Exception Scenario} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
-- If the message is empty, the request is not submitted.\newline
-- If an error occurs, an error message is displayed.
}\\ \hline
\end{tabular}
\end{table}

\subsubsection{e- Manage Conversations}
\textbf{Figure~\ref{fig:uc_manage_conv}} presents the use case diagram of Manage Conversations:

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Manage Conversation.jpg}
    \caption{Use Case Diagram of Manage Conversations}
    \label{fig:uc_manage_conv}
\end{figure}

\textbf{Table~\ref{tab:ref_manage_conv}} specifies the lifetime operations of conversational sessions.

\begin{table}[H]
\centering
\caption{Refinement of the Use Case Manage Conversations}
\label{tab:ref_manage_conv}
\begin{tabular}{|>{\centering\arraybackslash}p{4cm}|p{10cm}|}
\hline
\textbf{Use Case} & Manage Conversations \\ \hline
\textbf{Actor} & Administrator, Consultant, Document Manager \\ \hline
\textbf{Precondition} & User is authenticated. \\ \hline
\textbf{Postcondition} & Conversations are created, viewed, updated, or deleted successfully. \\ \hline
\textbf{Main Scenario} & The system displays the list of conversations. \\ \hline
\textbf{Alternative Scenarios} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
The user initiates a \textbf{New Conversation}:\newline
\hspace*{0.2cm}-- The system creates and adds it to the list.\newline
The user selects a \textbf{conversation} to view its content.\newline
The user searches for a conversation by title or preview.\newline
The user chooses to \textbf{edit} a conversation title:\newline
\hspace*{0.2cm}-- The user modifies the title and the system saves it.\newline
The user chooses to \textbf{delete} a conversation:\newline
\hspace*{0.2cm}-- The system requests confirmation before deletion.
}\\ \hline
\textbf{Exception Scenario} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
-- If no conversation exists, an empty state is displayed.\newline
-- If an error occurs while loading, an error message is displayed.
}\\ \hline
\end{tabular}
\end{table}

\subsubsection{f- Manage Documents}
\textbf{Figure~\ref{fig:uc_manage_docs}} presents the use case diagram of Manage Documents:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{Manage Documents.jpg}
    \caption{Use Case Diagram of Manage Documents}
    \label{fig:uc_manage_docs}
\end{figure}

\textbf{Table~\ref{tab:ref_manage_docs}} contains execution procedures for document ingestion and deletion.

\begin{table}[H]
\centering
\caption{Refinement of the Use Case Manage Documents}
\label{tab:ref_manage_docs}
\begin{tabular}{|>{\centering\arraybackslash}p{4cm}|p{10cm}|}
\hline
\textbf{Use Case} & Manage Documents \\ \hline
\textbf{Actor} & Administrator, Document Manager \\ \hline
\textbf{Precondition} & User is authenticated and the SharePoint API connection is established. \\ \hline
\textbf{Postcondition} & Documents are successfully ingested into the pipeline or deleted from the index. \\ \hline
\textbf{Main Scenario} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
The system connects to \textbf{SharePoint via API} and retrieves the folder structure.\newline
The system displays the list of available folders and documents fetched from SharePoint.
}\\ \hline
\textbf{Alternative Scenarios} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
The user selects a \textbf{folder} to ingest:\newline
\hspace*{0.2cm}-- The system uses the SharePoint API to retrieve all files within the folder.\newline
\hspace*{0.2cm}-- The system triggers the ingestion pipeline to process all retrieved files.\newline
The user selects a \textbf{single file} to ingest:\newline
\hspace*{0.2cm}-- The system triggers the ingestion pipeline to process the file.\newline
The user initiates a \textbf{Delete} \newline
\hspace*{0.2cm}-- The system requests confirmation before deletion.\newline
\hspace*{0.2cm}-- The system removes the document from the ingestion index.\newline
}\\ \hline
\textbf{Exception Scenario} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
-- If the SharePoint API is unreachable, an error message is displayed.\newline
-- If ingestion or pipeline processing fails, an error message is displayed.\newline
}\\ \hline
\end{tabular}
\end{table}

\subsubsection{g- Manage Metadata}
\textbf{Figure~\ref{fig:uc_manage_metadata}} presents the use case diagram of Manage Metadata:

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Manage Metadata.jpg}
    \caption{Use Case Diagram of Manage Metadata}
    \label{fig:uc_manage_metadata}
\end{figure}

\textbf{Table~\ref{tab:ref_manage_metadata}} specifies data refinement methods after automated file extraction.

\begin{table}[H]
\centering
\caption{Refinement of the Use Case Manage Metadata}
\label{tab:ref_manage_metadata}
\begin{tabular}{|>{\centering\arraybackslash}p{4cm}|p{10cm}|}
\hline
\textbf{Use Case} & Manage Metadata \\ \hline
\textbf{Actor} & Administrator, Document Manager \\ \hline
\textbf{Precondition} & Document is ingested and processed. \\ \hline
\textbf{Postcondition} & Metadata is completed and validated successfully. \\ \hline
\textbf{Main Scenario} & After document ingestion, the system displays the extracted metadata. \\ \hline
\textbf{Alternative Scenarios} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
The system automatically extracts metadata after document ingestion.\newline
The user completes missing fields such as author, type, or language.\newline
The user chooses to \textbf{correct} metadata:\newline
\hspace*{0.2cm}-- The system updates the modified fields.\newline
The user chooses to \textbf{validate} metadata:\newline
\hspace*{0.2cm}-- The system saves the validated metadata.
}\\ \hline
\textbf{Exception Scenario} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
-- If extraction fails, manual input is allowed.\newline
-- If required fields are missing, validation is blocked.\newline
-- If validation fails, an error message is displayed.
}\\ \hline
\end{tabular}
\end{table}
\newpage
\subsubsection{h- View Logs}
\textbf{Figure~\ref{fig:uc_view_logs}} presents the use case diagram of View Logs:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{view logs.jpg}
    \caption{Use Case Diagram of View Logs}
    \label{fig:uc_view_logs}
\end{figure}

\textbf{Table~\ref{tab:ref_view_logs}} establishes transparency structures via security logs.

\begin{table}[H]
\centering
\caption{Refinement of the Use Case View Logs}
\label{tab:ref_view_logs}
\begin{tabular}{|>{\centering\arraybackslash}p{4cm}|p{10cm}|}
\hline
\textbf{Use Case} & View Logs \\ \hline
\textbf{Actor} & Administrator, Document Manager \\ \hline
\textbf{Precondition} & User is authenticated and has access to audit logs. \\ \hline
\textbf{Postcondition} & Logs are displayed, filtered, and optionally exported. \\ \hline
\textbf{Main Scenario} & The system displays the list of audit logs. \\ \hline
\textbf{Alternative Scenarios} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
The user filters logs by category (documents, users, system, security).\newline
The user browses the logs list to view details such as action, user, timestamp, and description.\newline
The user initiates an \textbf{Export Logs}:\newline
\hspace*{0.2cm}-- The system generates and downloads the log file.\newline
The system automatically records each user or system action as a log entry.
}\\ \hline
\textbf{Exception Scenario} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
-- If no logs exist, an empty state is displayed.\newline
-- If an error occurs while loading, an error message is displayed.\newline
-- If export fails, the user is notified.
}\\ \hline
\end{tabular}
\end{table}
\newpage
\subsubsection{i- View Analytics}
\textbf{Figure~\ref{fig:uc_view_analytics}} presents the use case diagram of View Analytics:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{view analytics questions.jpg}
    \caption{Use Case Diagram of View Analytics}
    \label{fig:uc_view_analytics}
\end{figure}

\textbf{Table~\ref{tab:ref_view_analytics}} outlines the performance metric reporting workflow.

\begin{table}[H]
\centering
\caption{Refinement of the Use Case View Analytics}
\label{tab:ref_view_analytics}
\begin{tabular}{|>{\centering\arraybackslash}p{4cm}|p{10cm}|}
\hline
\textbf{Use Case} & View Analytics \\ \hline
\textbf{Actor} & Administrator \\ \hline
\textbf{Precondition} & User is authenticated and has admin privileges. \\ \hline
\textbf{Postcondition} & Analytics data is displayed and interpreted successfully. \\ \hline
\textbf{Main Scenario} & The system displays analytics dashboards including system performance and usage metrics. \\ \hline
\textbf{Alternative Scenarios} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
The system displays \textbf{model performance} including response accuracy and number of processed queries.\newline
The system displays \textbf{evaluation metrics per model}:\newline
\hspace*{0.2cm}-- \textbf{Latency}: average response time per model.\newline
\hspace*{0.2cm}-- \textbf{LLM cost per model}: token-based cost for each model.\newline
\hspace*{0.2cm}-- \textbf{Total cost}: aggregated cost across all models and queries.\newline
The user initiates an \textbf{Export Report}:\newline
\hspace*{0.2cm}-- The system generates and downloads the analytics report.
}\\ \hline
\textbf{Exception Scenario} &
\parbox[t]{10cm}{\small\setlength{\parskip}{1pt}
-- If no analytics data is available, an empty state is displayed.\newline
-- If an error occurs while loading, an error message is displayed.\newline
-- If charts fail to load, placeholders are displayed.
}\\ \hline
\end{tabular}
\end{table}

\section{Conceptual Study}
This section presents the architectural and dynamic views of the Ask-Jade system through deployment, activity, and sequence diagrams.

\subsection{Sequence Diagrams}
This section presents the sequence diagrams for the core use cases of Ask-Jade.

\subsubsection{a- Manage Document Sequence Diagram}
\textbf{Figure~\ref{fig:seq_manage_documents}} illustrates the sequence of
interactions between the different system components during the document
management process, covering document display, deletion, and ingestion flows.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{ManageDocs_page-0001.jpg}
\caption{Sequence Diagram --- Manage Documents Use Case}
\label{fig:seq_manage_documents}
\end{figure}

\subsubsection{b- Manage Metadata Sequence Diagram}
\textbf{Figure~\ref{fig:seq_manage_metadata}} illustrates the sequence of
interactions during the metadata management process, covering the display,
completion, and correction of document metadata fields.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{ManageMetadata_page-0001.jpg}
\caption{Sequence Diagram --- Manage Metadata Use Case}
\label{fig:seq_manage_metadata}
\end{figure}

\subsubsection{c- Manage Users Sequence Diagram}
The complete sequence diagram of the Manage Users use case is provided in
Appendix~\ref{annexe:seq_users}.








\subsection{Activity Diagrams}
This section presents the activity diagrams for the two core use cases of Ask-Jade: asking a question and ingesting a document.

\subsubsection{a- Ask Question Activity Diagram}
\textbf{Figure~\ref{fig:activity_ask_question}} illustrates the activity flow of the RAG pipeline, from query submission to answer generation.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{questionActivity.jpg}
    \caption{Activity Diagram --- Ask Question Use Case}
    \label{fig:activity_ask_question}
\end{figure}

\subsubsection{b- Document Ingestion Activity Diagram}
\textbf{Figure~\ref{fig:activity_ingestion}} illustrates the activity flow of the document ingestion pipeline, from file detection to vector storage.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ActivityDiagram1.jpg}
    \caption{Activity Diagram --- Document Ingestion Use Case}
    \label{fig:activity_ingestion}
\end{figure}

\subsection{Class Diagram}
\textbf{Figure~\ref{fig:class_diagram}} illustrates the logical structural architecture of the persistent entities within Ask-Jade.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{schemaFinalCouleur.jpg}
    \caption{Class Diagram of the Ask-Jade System}
    \label{fig:class_diagram}
\end{figure}

Users are the central entity of the system, managing documents, projects, chat sessions, and generating logs. Documents represent uploaded files with full metadata and are split into chunks small text units enriched with vector embeddings and full-text search indexes that power the RAG pipeline. Documents can optionally belong to a project, which groups work by client, sector, and country. Each user can run multiple chat sessions, each containing ordered chat messages from both the user and the AI, also stored with embeddings for contextual retrieval. Finally, every user action is recorded in the logs table, capturing the action type, target entity, status, and IP address to ensure full system traceability.

\section{Conclusion}


This chapter presented the requirements analysis and conceptual design of the Ask-Jade system. It defined the main functional and non-functional requirements, as well as the roles of the Consultant, Document Manager, and Administrator. These aspects were modeled using UML diagrams.

The next chapter, will cover the theoretical background, including LLMs, RAG architectures, and evaluation metrics used to assess system performance.