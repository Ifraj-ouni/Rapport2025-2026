\chapter{Ask-Jade Pipeline}

% ═══════════════════════════════════════════════════════════════════════════════
\section{Introduction}
% ═══════════════════════════════════════════════════════════════════════════════

This chapter describes the technical architecture of the Ask-Jade system.
It presents the ingestion pipeline, which transforms raw documents into
a searchable knowledge base, and the retrieval pipeline, which processes
user queries and generates grounded, cited responses. The rationale behind
each technological choice is also discussed.

% ═══════════════════════════════════════════════════════════════════════════════
\section{Pipeline Architecture Overview}
% ═══════════════════════════════════════════════════════════════════════════════

To address the challenges described in previous chapters, a modular
Retrieval-Augmented Generation (RAG) pipeline was designed to structure
and exploit unstructured documents. As established in the comparative analysis of Chapter~2
(Table~\ref{tab:llama_langchain}), LlamaIndex~\cite{llamaindex} was selected
as the main orchestration framework over LangChain, due to its native
optimisation for document indexing, structured retrieval, and compatibility
with Docling and pgvector all of which are central components of the
Ask-Jade pipeline.

The pipeline is organised into two complementary pipelines:

\begin{itemize}
  \item The \textbf{Ingestion pipeline} operates offline, before any user
        interaction. It processes documents by parsing and splitting extracted text into  chunks, generates embeddings, and
        persists everything in the vector database, all via LlamaIndex ingestion pipeline.

  \item The \textbf{Retrieval pipeline} operates at query time via the
        LlamaIndex Condense Plus Context Chat Engine. It rewrites
        the user query from conversation history, expands it into multiple
        variants, retrieves relevant chunks, merges results via RRF Fusion,
        reranks candidates, and produces a grounded, cited answer using a configurable Large Language Model. Every generation trace is saved to
        Langfuse for observability.
\end{itemize}



The following sections detail each pipeline in turn.

% ═══════════════════════════════════════════════════════════════════════════════
\section{Ingestion Pipeline}
\label{sec:ingestion}
% ═══════════════════════════════════════════════════════════════════════════════

The ingestion pipeline transforms raw, heterogeneous documents into structured,
searchable knowledge. It covers four stages: acquisition, processing,
embedding generation, and vector storage. \textbf{Figure~\ref{fig:ingestion_pipeline}}
illustrates this flow.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{IngestionPipeline.png}
  \caption{Ingestion pipeline of the Ask-Jade system}
  \label{fig:ingestion_pipeline}
\end{figure}

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Document Acquisition}
% ─────────────────────────────────────────────────────────────────────────────
 
The system currently supports three document formats:
PDF, DOCX and PPTX.
All documents come from the SharePoint connector,
in which the user selects the files to ingest. This insures that  a single data source (source of truth) is maintained .
 
Prior to entering the pipeline, each document is checked for near-duplicates
via TLSH (Trend Micro Locality Sensitive Hash). Unlike cryptographic
hashes such as SHA-256, which only detect exact byte-for-byte duplicates,
TLSH computes a fuzzy fingerprint over the document content and measures
similarity via a distance score. Two documents whose TLSH distance falls below
a defined threshold are considered near-duplicates; the incoming document is
then discarded. Otherwise it is forwarded to the next stage. This approach
prevents redundant storage even when documents differ only by minor edits,
metadata updates, or formatting changes.

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Document Processing}
% ─────────────────────────────────────────────────────────────────────────────

DOCX files are first converted to PDF to standardise the processing workflow.
All documents are then processed by Docling~\cite{docling},
an open-source document-understanding library developed by IBM Research.
\textbf{Table~\ref{tab:docling_comparison}} compares Docling with
PyMuPDF~\cite{pymupdf} and pdfplumber~\cite{pdfplumber}.

\begin{table}[H]
\centering
\caption{Comparison of document parsing tools}
\label{tab:docling_comparison}
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{Feature} & \textbf{Docling} & \textbf{PyMuPDF} & \textbf{pdfplumber} \\
\hline
PDF support          & \checkmark & \checkmark & \checkmark \\
DOCX support         & \checkmark & \texttimes & \texttimes \\
PPTX support         & \checkmark & \texttimes & \texttimes \\
Layout analysis      & Advanced   & Basic      & Moderate   \\
Table extraction     & Strong     & Limited    & Strong     \\
OCR support          & Yes        & No         & No         \\
Metadata extraction  & Yes        & Limited    & No         \\
\hline
\end{tabular}
\end{table}

Docling performs two main operations on each document:

\begin{enumerate}
  \item \textbf{Text extraction} extracts raw text , table structures from documents. For scanned or image-based content, OCR is applied when needed. All while preserving the logical reading order, including complex multi-column layouts.
  \item \textbf{Chunking} splits the extracted text using the Docling HybridChunker which performs structure-aware chunking while enforcing a maximum number of tokens. It also captures file metadata such as the source file name , page numbers and section titles. This metadata is attached automatically to every chunk and is used later for citation generation in the final answer.
\end{enumerate}

The resulting chunks are then sent to the embedding model, as described in the following section.

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Embedding Generation}

In the embedding generation step, each chunk is transformed into a dense
numerical vector that encodes its semantic content in a continuous
high-dimensional space, enabling similarity-based retrieval even without
exact keyword overlap.

Based on the comparative analysis conducted in Chapter~2, \textbf{BGE-M3}~\cite{bge_m3},
developed by the Beijing Academy of Artificial Intelligence (BAAI), was
selected as the embedding model for Ask-Jade. Its hybrid retrieval approach combining dense, sparse, and multi-vector representations along with
its state-of-the-art performance on multilingual benchmarks such as MIRACL
and MKQA, make it particularly well suited for the multilingual and
domain-specific nature of Jade Advisory's document base.
\textbf{Table~\ref{tab:bge_m3_specs}} summarises its key technical specifications.

\begin{table}[H]
\centering
\caption{BGE-M3 Technical Specifications}
\label{tab:bge_m3_specs}
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{|l|l|}
\hline
\textbf{Property} & \textbf{Value} \\
\hline
Languages supported & 100+ (including French, English, Arabic) \\
\hline
Max input length & 8{,}192 tokens \\
\hline
Embedding dimension & 1{,}024 \\
\hline
Retrieval modes & Dense, Sparse (lexical), Multi-vector (ColBERT) \\
\hline
\end{tabular}
\end{table}

As shown in \textbf{Table~\ref{tab:bge_m3_specs}}, BGE-M3 combines broad multilingual
coverage with flexible retrieval modes, confirming its suitability for
enterprise-level RAG applications. In Ask-Jade, dense embeddings are used
as the primary retrieval representation, capturing global semantic meaning
across French, English, and Arabic documents.











% ─────────────────────────────────────────────────────────────────────────────
\subsection{Vector Storage}
% ─────────────────────────────────────────────────────────────────────────────

Chunk embeddings are stored in a PostgreSQL database extended with both the pgvector and tsvector capabilities, enabling hybrid retrieval. Pgvector is used for semantic retrieval through vector similarity search, while tsvector supports keyword-based full-text search.
PostgreSQL was chosen because it is already part of the existing infrastructure, avoiding the operational overhead of introducing an external search service.
Efficient similarity search is enabled using an Approximate Nearest Neighbor (ANN) index based on Hierarchical Navigable Small World (HNSW) graphs, which allows fast retrieval of semantically similar chunks even in large-scale collections.

% ═══════════════════════════════════════════════════════════════════════════════
\section{Retrieval Pipeline}
\label{sec:retrieval}
% ═══════════════════════════════════════════════════════════════════════════════

The retrieval pipeline processes a user query through six sequential stages and
returns a grounded, cited response. \textbf{Figure~\ref{fig:retrieval_pipeline}}
illustrates the full flow.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{RetrievalPipeline.png}
  \caption{Retrieval pipeline of the Ask-Jade system}
  \label{fig:retrieval_pipeline}
\end{figure}

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Query Rewriting (Condense Step)}
% ─────────────────────────────────────────────────────────────────────────────
In multi-turn conversations, the Condense Step reformulates the user's
question into a self-contained query using the conversation history.
For single-turn interactions, the query is forwarded unchanged.

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Multi-Query Generation}
% ─────────────────────────────────────────────────────────────────────────────

To improve semantic coverage, the reformulated query is expanded into 2 to 3
variants by an LLM. Because the same information can be expressed in multiple
ways across source documents, each variant is independently embedded and used
to query the vector store, broadening the candidate set beyond what a single
formulation would return. This approach also helps simplify retrieval for complex, composed questions.

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Semantic Retrieval}
% ─────────────────────────────────────────────────────────────────────────────

Each query variant is encoded with BGE-M3 the same model used during
ingestion ensuring alignment in the shared semantic space. Similarity is
measured by cosine distance in PgVector, and the top-10 most similar chunks
are retrieved per variant, yielding 20 to 30 candidates in total. Dense
retrieval handles both conceptual queries and paraphrased questions equally
well, and naturally supports cross-lingual search (e.g.\ a French query
retrieving English source material).

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Reciprocal Rank Fusion (RRF)}
% ─────────────────────────────────────────────────────────────────────────────

The $N$ ranked lists are merged using Reciprocal Rank Fusion (RRF),
which assigns each document a score inversely proportional to its rank: The RRF score equation can be found at equation ~\ref{eq:rrf_score}.

\begin{equation}
\mathrm{RRF\_score}(d) =
\sum_{i=1}^{N} \frac{1}{k + \mathrm{rank}_i(d)}
\label{eq:rrf_score}
\end{equation}

where $k = 60$ is a smoothing constant. 

RRF is needed because each query variant produces a different ranking of results and similarity scores are not comparable across queries. Instead of averaging scores, it merges results based on their ranks, producing a more stable final ranking that favors documents consistently ranked at the top.

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Reranking}
% ─────────────────────────────────────────────────────────────────────────────

The fused list is passed to BGE-reranker-v2-m3, a cross-encoder that
jointly processes the query and each candidate chunk. Unlike the bi-encoder
used at retrieval time, the cross-encoder achieves finer relevance scoring and
reduces noisy results. Only the top-5 chunks are forwarded to answer
generation.

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Answer Generation}
\label{sec:llm_generation}
% ─────────────────────────────────────────────────────────────────────────────

The final stage generates a grounded response from the top-5 reranked chunks
and the user query. A key design requirement is flexibility, allowing the administrator to
switch the generation model directly from the application interface no
code change or redeployment is needed allowing the organisation to adopt
new releases, control costs, or adjust to evolving performance benchmarks.

The default LLM is \textbf{gpt-5.4-mini} because it provides the best balance between
reasoning capability, multilingual understanding, and large-context processing, making it suitable
for professional document-based retrieval in the PPP consulting domain.

All supported models share a common provider abstraction layer: they receive
the same structured prompt and return a response in the same format. Switching
the active model therefore requires only updating a single configuration value
in the database. The prompt contains three elements:

\begin{enumerate}
  \item \textbf{System instruction} defines the assistant's role, output
        format, response language, and citation rules and critical constraints such as safety and grounding requirements. 
  \item \textbf{Retrieved context} the top-5 reranked chunks, each
        annotated with its source document, page number, and section title.
  \item \textbf{User query} the original question as submitted.
\end{enumerate}

The model is instructed to answer exclusively from the provided context, cite
every factual claim with its source chunk, and respond in the same language as
the user's query (French, English). The result is a grounded
response with traceable citations linked to the original document chunks.
The full generation prompt is provided in Appendix~\ref{annexe:generation_prompt}.

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Observability with Langfuse}
% ─────────────────────────────────────────────────────────────────────────────

After each answer is generated, the complete prompt trace system
instruction, retrieved context, user query, and model response is saved to
Langfuse ~\cite{langfuse}. This provides full observability over the
generation pipeline, enabling the team to inspect LLM calls, monitor token
usage and costs, evaluate answer quality using it's built in LLM-as-a-Judge capabilities, and debug unexpected outputs without
modifying any application code.



\section{Conclusion}
This chapter presented the two core pipelines of the Ask-Jade system.
The ingestion pipeline handles document acquisition, processing, embedding
and vector storage, while the retrieval pipeline processes user queries
through rewriting, multi-query expansion, semantic retrieval, fusion,
reranking, and answer generation and the langfuse observability. Together, these components form a
modular and scalable RAG architecture. The following chapter will present
the technical specification, the system interfaces, and the evaluation
of the final architecture.